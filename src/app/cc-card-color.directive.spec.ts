import { CcCardColorDirective } from './cc-card-color.directive';

describe('CcCardColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CcCardColorDirective();
    expect(directive).toBeTruthy();
  });
});
